#include "config.h"
#include "logger.h"

/*
 * Expected options:
 *	-l	set log level (2 by default)
 *	-c	set config
 *	-h	show help
 */

int main (int argc, const char **argv) {
	/*
	const char *prog_name = argv[0];

	int i = 0;
	for (; i < argc; ++i) {
		
	}
	*/

	return 0;
}
